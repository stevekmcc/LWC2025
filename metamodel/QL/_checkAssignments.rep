_checkAssignments(@questionId)

foreach .(*Question); where :Identifier = @questionId
{
	if ~StorePart; then
		$readOnly = 'true'
	endif
}

foreach .QuestionnaireExtension
{
	do decompositions { _checkAssignments(@questionId) }
}

